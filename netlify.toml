[build]
  PYTHON_VERSION = "3.8"
  command = "pip install --upgrade pip && pip install -r requirements.txt"
  publish = "public"  # Remplacez "public" par le dossier de publication de votre application (souvent "build" ou "dist" pour certains projets)

